import React from 'react';

const Single = (props) => {
    const {title, score, backdrop, poster, overview, voteCount, genres, date, isMovie, } = props
    return (
        <div>
            
        </div>
    );
};

export default Single;